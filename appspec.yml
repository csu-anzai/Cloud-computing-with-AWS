version: 0.0
os: linux

files:             
  - source: ~/
    destination: /home/centos

hooks:
  AfterInstall:
   - location: ./afterInstall.sh
     timeout: 180
     runas: centos
  ApplicationStart:
    - location: ./startApp.sh
      timeout: 3600
      runas: centos
